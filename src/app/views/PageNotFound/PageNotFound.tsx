import styles from './PageNotFound.module.css';

const PageNotFound = () => (
  <div>
    <h1 className={styles.nf_title}>404</h1>
    <p className={styles.nf_subtitle}>page not found</p>
  </div>
);

export default PageNotFound;
